<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/loading_dialog_root_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <android.support.v7.widget.CardView
        android:id="@+id/loading_dialog_card_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:cardCornerRadius="8dp"
        android:layout_gravity="center">

        <LinearLayout
            android:id="@+id/loading_dialog_linear_layout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingStart="64dp"
            android:paddingTop="16dp"
            android:paddingEnd="64dp"
            android:paddingBottom="16dp"
            android:orientation="vertical">

            <ProgressBar
                android:id="@+id/loading_dialog_progress_bar"
                android:layout_width="56dp"
                android:layout_gravity="center"
                android:indeterminate="true"
                android:theme="@style/AppTheme.AccentProgressBar"
                android:layout_height="56dp" />

            <TextView
                android:id="@+id/loading_dialog_text_view_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:textSize="16sp"
                android:textStyle="bold"
                android:text="Please wait..."
                android:padding="8dp" />

        </LinearLayout>

    </android.support.v7.widget.CardView>

</FrameLayout>